# Атланты — PvP-прототип. Файл передачи проекта

Этот файл — полный контекст проекта. По нему можно продолжить работу с нуля,
даже открыв в другой сессии/учётке Claude. Читать целиком перед изменениями.

---

## 1. Что это за проект

**«Атланты»** — коллекционная карточная игра (жанр card battler в духе Slay the Spire),
которую делают для размещения в мобильном приложении **«Клуб Друзей»** (сеть магазинов СПАР)
в формате веб-вью.

**Идея продукта:** покупатель получает физическую карту с QR (бесплатно при чеке от 1000 ₽
или покупает на кассе), сканирует QR в приложении — карта попадает в его цифровую колоду.
Колодой сражается в дуэлях. Идентификатор игрока — его id в «Клубе Друзей». Колода хранится
на сервере, привязана к id. На старте выдаётся слабая стартовая колода.

**Что сделано на данный момент:** играбельный **веб-прототип** (папка `proto/`) —
с настоящими картами и правилами из оригинальной игры, разложенный по экранам
из схемы в Figma (раздел 13): главный экран, PvE, колоды, редактор, сканирование, профиль.
Бой идёт против скрипта (бота).

**Мультиплеер работает целиком:** лобби со списком игроков, вызов по id, бой
считает сервер, колоды и коллекция лежат на сервере, личность — из подписанного
токена «Клуба Друзей». Подробности — разделы 14.6–14.9.

---

## 2. Где что лежит

| Что | Где |
|---|---|
| **GitHub-репозиторий** | https://github.com/kstnkr-rgb/atlants2 (ветка `main`, публичный) |
| **Играбельная ссылка (бот)** | https://raw.githack.com/kstnkr-rgb/atlants2/main/proto/index.html |
| **Онлайн вдвоём** | нужен запущенный `relay.js`: игра лежит на `<адрес сервера>/proto/`. С githack тоже работает, если добавить `?server=<адрес сервера>` |
| **Локальная папка проекта** | `C:\Users\Konstantin\Documents\claude projects\atlanteans-pvp` |
| **Исходный Unity-проект игры** | `C:\Users\Konstantin\Downloads\aha-game-client-main` |
| **Figma (макет боя)** | файл `4zRCI1PJKXiaghf9hRB8tw`, фрейм боя node `11:10379`, новые иконки node `39:345` |

**Важно про githack/кэш:** `raw.githack.com` агрессивно кэширует. После пуша обновлять
страницу через **Ctrl+F5**, иначе видна старая версия. Это объясняло часть «багов».

---

## 3. Структура репозитория

```
proto/                     ← ГЛАВНОЕ: текущий прототип (экраны по схеме + бой с ботом)
  index.html               экраны, отрисовка, перетаскивание карт — интерфейс
  rules.js                 ПРАВИЛА БОЯ без DOM: общий модуль для клиента и сервера
  net.js                   клиент PvP: рисует вид от сервера и шлёт намерения
  cards.js                 база 84 карт (из Unity Remote Config) + стартовая колода
  proto-test.js            headless-тест логики (Node, 71 проверка, стабит DOM)
  manifest.json            PWA-манифест (запуск с домашнего экрана без адресной строки)
  img/                     графика из Figma: bg, zeus, athena, портреты, рамки, hpbar, иконки
                           + menu_bg, scan_bg, avatar — из старой игры (лист
                           «старые атланты»); logo и icon_profile — из макета
                           главного экрана на листе «схема»
  img/cards/               84 арта карт (a1..a28, d1..d28, m1..m28), нарезаны из атласов Unity

matches.js                 СЕРВЕР МАТЧЕЙ: бой считается тут, действия проверяются
lobby.js                   ЛОББИ: присутствие, вызовы по id, согласование колод
auth.js                    ЛИЧНОСТЬ: проверка подписанного токена от МП, сессии
store.js                   ХРАНИЛИЩЕ: коллекция карт и колоды игрока (data/players.json)
match-test.js              55 проверок сервера матчей (npm run test:match)
lobby-test.js              41 проверка лобби (npm run test:lobby)
store-test.js              33 проверки личности и хранилища (npm run test:store)
relay.js                   HTTP: /api/match/* + раздача статики + старый релей (npm start)

public/index.html          СТАРЫЙ веб-арбитр PvP по коду комнаты (2 живых игрока) — демо-карты
server.js, engine.js       СТАРЫЙ сервер-арбитр (Node, комнаты) — под старые демо-карты
cards.json, test-engine.js относятся к старому арбитру (npm run arbiter / test:arbiter)

data/
  remote-config.csv        полная выгрузка Unity Remote Config (карты, враги, экономика и т.д.)
  cards-real.json          80 карт, распознанных с фото-карточек (сверка, справочно)

Атланты — концепция игры.docx      концепт-документ (для показа)
Атланты — презентация.pptx         презентация (4 слайда)
README.md                          краткое описание
```

**Ключевой момент архитектуры прототипа:** правила боя вынесены из разметки
в **`proto/rules.js`** — модуль без DOM, без глобального состояния и без анимации.
Состояние передаётся аргументом, всё «показать игроку» — объектом `fx`.
Тот же файл подключит сервер (раздел 14). В `index.html` остался интерфейс
и тонкие обёртки, подставляющие текущее состояние.

---

## 4. Правила боя (перенесены из движка оригинальной игры)

Источник правды — код оригинала в `aha-game-client-main/Assets/_SPAR/_Scripts/Runtime/Battle`
(`BattleAPI.cs`, `FighterBehaviour.cs`, `PlayerBehaviour.cs`, `Commands/*`).

- Здоровье **50** (`MAX_HP`), энергия **3** за ход (`MAX_NRG`), рука **5** карт (`HAND_SIZE`).
  (В оригинале старт 90 HP; понизили до 50 для скорости, но настоящие карты бьют сильно —
  бои по 3–7 ходов. Вопрос «вернуть 90» остаётся открытым.)
- **Урон** = базовый + сила атакующего; гасится блоком, остаток в HP.
- **Блок** = базовый + ловкость; сгорает в начале СВОЕГО хода (если нет переноса).
- **Энергия** восстанавливается до максимума каждый ход, не копится (если нет переноса).
- **Сила/ловкость** — постоянные баффы до конца боя (обнуляются только при старте боя).
- **Перенос блока/энергии** (знак `!` в конфиге): остаток переносится на N ходов
  (счётчики `trBlock`/`trNrg`; есть карты на 10 ходов).
- **Дебафф силы/ловкости** (наш выбор, отход от оригинала): действует **1 ход** жертвы,
  потом снятое возвращается. Хранится списком `debuffs:[{stat,amount,wait}]` — у каждого
  свой срок (наложенный позже не продлевает ранний). Нижней границы нет (уходит в минус).
- **Умножение** (знак `*`): ×2/×3 силы/ловкости/блока/энергии. Защита: умножение
  **отрицательного** значения силы/ловкости ничего не делает (иначе была лавина −64).
- **Изгнание** (Disappear): карта после розыгрыша уходит из боя (`exhaust:true`).
- Пустая колода перетасовывается из сброса. Урон никогда не отрицательный.

---

## 5. Система карт

**84 карты**, параметры взяты из **Unity Remote Config** (ключи `cardsAttack`/`cardsDefend`/
`cardsMagic`), а не из ручной расшифровки. База — `proto/cards.js`.

- **Ключ карты = имя спрайта:** `a2`=Attack2, `d6`=Defend6, `m28`=Magic28. Арт — `img/cards/<ключ>.png`.
- Нумерация набора: атака 1–28, защита 29–56, магия 57–84 (85–100 — карты врагов, не заведены).
- **Улучшенные версии карт** (`cardUpgradedEffects` в конфиге) НЕ используются — только базовые
  (по решению заказчика; каждая карта — один вариант).
- Формат карты: `{ no, type, cost, title, tpl, fx:[...], exhaust? }`.
  `tpl` — человеческий текст (справочно, в UI не показывается).
  Описание в UI генерируется из `fx` функцией `cardDesc()`: компактно, знаками и иконками.
- **Типы эффектов `fx.k`:** `dmg` (урон), `hpFoe` (прямой урон мимо блока), `hpSelf` (себе/лечение),
  `blk`, `str`/`dex`, `strFoe`/`dexFoe`, `mulStr`/`mulDex`/`mulBlk`/`mulNrg`, `nrg`, `draw`,
  `trBlock`/`trNrg` (перенос), `copyHand`/`copySelf`/`copyDeck`.
- **Перевод знаков конфига** (в генераторе): `''`→установить/базовый, `+`/`-`→плюс/минус,
  `*`→умножение, `!`→перенос. targetType Caster/Itself → игрок; ChosenTarget/Enemies/Player → враг.
- **Как пересобрать `cards.js` из конфига:** парсить `data/remote-config.csv`, взять `cardsAttack`/
  `cardsDefend`/`cardsMagic`, поле `cardEffects` (НЕ `cardUpgradedEffects`), маппить по таблице выше.

**Арт карт** нарезан из атласов Unity `Assets/_SPAR/UI/Cards` (`Attack1-24.png`+`.tpsheet` и т.д.).
Внимание: экспорт/нарезка Figma и Unity склеивает картинки с тёмным фоном холста (#1E1E1E) —
прозрачность надо восстанавливать flood-fill; координаты спрайтов в `.tpsheet` считаются от
НИЗА текстуры (`crop(x, H-y-h, x+w, H-y)`).

---

## 6. Колоды и редактор

- Боевая колода — **20 карт**, максимум **3 копии** одной карты (`MAX_COPIES`, из `cardAddingLimit`).
- **Редактор колод:** 20 слотов (10 в ряд) сверху, ниже все 84 карты (10 в ряд, прокрутка).
  Клик по карте — добавить, по слоту — убрать. Счётчик копий `×N`.
- **Стартовая колода** (`STARTER_DECK` в cards.js) — из `startDeck` конфига, продублирована до 20:
  Копьё×8, Щит×8, Ксифос×2, Гоплон×2. Заведена как обычная редактируемая колода с меткой СТАРТ,
  удалить нельзя. Есть миграция: старая 10-карточная в localStorage дополняется до 20.
- **Пресеты:** Натиск / Оборона / Энергия — засеиваются один раз.
- **Хранение колод — localStorage** (ключ `atlanteans_decks`). НЕ на сервере — на другом
  устройстве колод нет. Есть перенос строкой: кнопки «Скопировать»/«Вставить» (код вида
  `ATL1:Название:9,9,8,...`).
- **Экран выбора:** у каждой колоды кнопки «мне»/«врагу» — можно задать колоды обеим сторонам
  (для отладки). Соперник иначе берёт случайную из сохранённых. Бой при загрузке НЕ стартует —
  только по кнопке «Играть».

---

## 7. Интерфейс прототипа (что уже сделано)

- Сцена собрана в размерах фрейма Figma (2597.4×1568.8), масштабируется целиком — пропорции
  графики сохранены. Персонажи из исходников с прозрачностью; Афина отражена (`--fx:-1`).
- Розыгрыш карты — перетаскиванием в центр экрана. Ход бота: его карта показывается в центре,
  потом срабатывает эффект.
- **Энергия — цифрой 0–999** в синей «пилюле» (не секторами).
- **Описание карт:** компактное, из `fx`, иконки вместо слов параметров, выравнивание по левому
  краю. Текст 27px **жирный**. **Цифра урона — слева и крупнее в 1.5 раза** («12 урон врагу»).
  Бонус силы/ловкости подсвечивается зелёным (`.buffed`).
- **Индикатор переноса** блока/энергии под характеристиками (иконка + число ходов).
- **Отладочный лог** (кнопка ≡): все действия, эффекты подробно, сводка состояния, спад дебаффов.
- **Просмотр колоды** по клику на стопку: группировка по типу, сортировка по стоимости,
  карты руки помечены.
- **Мобильное:** touch-action для перетаскивания и прокрутки, подгон по высоте экрана,
  просьба повернуть в портрете, полноэкранный режим (кнопка + PWA-манифест).
- **HP-бар** под аватаром, без полупрозрачной подложки.

**Тесты:** `node proto/proto-test.js` — headless-прогон логики (стабит DOM), 49 проверок.
Запускать после любой правки движка. Тестовая партия использует фиксированную агрессивную
колоду (случайные колоды с копиями+переносом энергии могут зациклиться — артефакт жадного бота).

---

## 8. Новые иконки (НЕ подключены)

В `proto/img/` лежат `icon_block.png`, `icon_str.png`, `icon_dex.png` — новые круглые значки
из Figma (Броня — синий шлем, сила — красные мечи, ловкость — зелёный щит). **В игре пока
используются старые `.svg`** — по просьбе заказчика подключение отложено. Чтобы подключить:
заменить в `index.html` ссылки `icon_*.svg` → `icon_*.png` (в HUD у обоих бойцов и в `IC` для
описаний карт).

---

## 9. Что НЕ сделано (следующие большие задачи)

1. **Правильный серверный арбитр — РЕШЕНО ДЕЛАТЬ, см. раздел 14.** Заказчик выбрал
   синхронный PvP; обсчёт боя и колоды переезжают на сервер. Существующий онлайн
   «поиграть с другом» (раздел 12) — временный: движок в браузере хоста, хост доверенный.
   Оценка: ~2–3 недели.
2. **Хранение колод на сервере** (сейчас localStorage). Решено: колоды на сервере,
   привязаны к id «Клуба Друзей». Часть работ по разделу 14.
3. **Интеграция с «Клубом Друзей»:** со своей стороны сделано — сервер принимает
   подписанный токен, ТЗ для их команды лежит в разделе 14.9. Осталось согласовать
   секрет и доступ к камере для сканирования QR.
4. **Сканирование QR** карт камерой и реестр активации: «QR → карта → кому и когда выдана»,
   одноразовая привязка к первому отсканировавшему.
   **Ручного ввода кода не будет** (решение заказчика): иначе номера карт подбираются перебором.
5. **Карты врагов (85–100) и режим PvE** — данные врагов есть в `remote-config.csv`.
6. **Баланс:** решить про здоровье (50 vs 90). Конфликт: 20 карт из 4 базовых vs лимит 3 копии.

---

## 10. Ключевые факты об оригинальной игре (Unity)

- Unity **2021.3.8f1**, AR через **Vuforia** (сканирование карт камерой), Firebase, Addressables.
- ~34 000 строк своего кода. Клиент-серверная: бэкенд `https://game-api.r-proc.net/`
  (на момент разбора отвечал **503** — приложение за nginx остановлено; доступ к серверу у
  заказчика ЕСТЬ, можно чинить).
- Контент (3D-модели) — Addressables в Yandex Cloud
  (`storage.yandexcloud.net/aha-addressables-production`, живо).
- **Весь контент игры — в Unity Remote Config** (облако Unity Gaming Services). Организация
  `r-proc`, проект **Atlanteans**, `cloudProjectId 0df55c56-55f0-45cb-9975-84084e736d57`.
  Доступ к Remote Config у заказчика ЕСТЬ (выгрузка — кнопка Download в панели). Полная
  выгрузка сохранена в `data/remote-config.csv`.
- **Редкости карт в данных НЕТ** (`cardsAvailability` — одно число 240). «Низкая/высокая
  редкость» из концепта = базовая/улучшенная версия карты (`cardUpgradedEffects`).
- **Риск релиза:** нужен пароль от `magic-quick.keystore` (подпись Android) — без него нельзя
  обновить приложение в Google Play. Проект от января 2023 — при релизе в сторы возможен
  апгрейд Unity/SDK (недели работы).

---

## 11. Как продолжить работу (для новой сессии Claude)

1. Клонировать репозиторий или открыть локальную папку (раздел 2).
2. Прочитать этот файл целиком.
3. Прототип запускается статикой: `cd proto && python -m http.server 8777` → `localhost:8777`.
   Или открыть githack-ссылку. Двойным кликом по `index.html` НЕ работает (CORS для картинок).
4. После правок движка — прогнать `node proto/proto-test.js` (должно быть «все проверки прошли»).
5. Правила пользователя (важно!): **вносить только запрошенные изменения**; на вопрос —
   сначала ответить; своё улучшение — предложить вопросом, не делать молча. Версии не путать,
   коммитить по-русски осмысленными сообщениями, пушить в `origin/main`.
6. Графику и данные из Figma/Unity тянуть через соответствующие MCP-инструменты (Figma) и
   Python/PIL (нарезка атласов, восстановление прозрачности).

---

## 12. Онлайн-бой вдвоём (как устроен)

**Схема:** хост считает бой, релей передаёт, гость рисует.

- `relay.js` правил игры НЕ знает. Он держит комнату по 4-значному коду, хранит последний
  снимок состояния от хоста и очередь действий от гостя. Комната живёт 6 часов, лежит
  в памяти процесса (перезапуск сервера обрывает бои). Заодно раздаёт статику репозитория,
  чтобы игра и API были на одном адресе. Порт — `process.env.PORT` (Railway) или 3000.
- **Ключевой приём:** хост отдаёт снимок **уже перевёрнутым** — свои и чужие местами.
  Поэтому у гостя весь существующий рендер (`S.p[0]` — «я») работает без единой правки.
  Переворот в `flip()` в `proto/net.js`.
- Чужая рука и стопки в снимке обезличены (`blind()`): видно только количество карт.
- Гость движок не считает вообще. Он шлёт действия `{t:'play'|'take'|'end'|'again'}`,
  хост применяет их в `applyRemote()` тем же кодом, что и свои ходы (`playBy`).
- Опрос — раз в 800 мс в обе стороны. Снимок шлётся не на каждый `render()`,
  а по флагу «грязно» раз в такт. Пока карту тянут пальцем, снимок не применяется,
  иначе жест обрывается.
- В шапке вместо «ПРОТОТИП ДУЭЛИ» показывается код боя; если соперник не выходил
  на связь больше 10 секунд — «СОПЕРНИК ОФФЛАЙН».

**API релея:** `POST /api/create {deck}` → `{code, token}`; `POST /api/join {code, deck}` →
`{token}`; `POST /api/push {code, token, snap}` (только хост); `POST /api/act {code, token, act}`
(только гость); `GET /api/poll?code&token&ack|rev` — хосту отдаёт колоду гостя и его действия,
гостю — свежий снимок.

**Чего в этом варианте нет:** переподключения (перезагрузил страницу — бой потерян),
защиты от жульничества хоста, колод на сервере, подбора соперника. Оба игрока
по-прежнему видят себя слева как ZEUS — имена бойцов зашиты в вёрстку.

Без `net.js` игра работает как раньше, против бота: в `index.html` лежит заглушка `NET`.

---

---

## 13. Экраны прототипа (по схеме из Figma)

Схема — лист **«схема»** в файле «Атланты 2», фрейм **«схема экранов — графика»**
(рядом лежит исходный вайрфрейм «схема экранов»). Фреймы 2597,4 × 1568,8 — как боевой экран.
Оформление снято со старой игры: бордовая шапка, золотая линия, стрелка «назад».

| Экран | id в `index.html` | Что делает |
|---|---|---|
| Главный | `menuScreen` | Фон и бойцы как в бою, логотип «Дуэли богов», кнопки КОЛОДА / PvP / PvE, профиль в углу. **Шапки нет** |
| PvE выбор колоды | `pveScreen` | список колод с выбором «мне»/«врагу», кнопка ИГРАТЬ |
| PvP лобби | `pvpScreen` | **пусто** — мультиплеер выключен |
| Экран колод | `decksScreen` | список колод, «изменить», «✕», СОЗДАТЬ КОЛОДУ, ОТСКАНИРОВАТЬ КАРТУ |
| Редактор колоды | `deckEditor` | без изменений: 20 слотов, 84 карты, лимит 3 копии |
| Сканирование карты | `scanScreen` | видоискатель; ручного ввода кода НЕТ (см. ниже) |
| Профиль | `profileScreen` | аватар, имя, побед/поражений (в localStorage `atlanteans_profile`) |

**Главный экран собран в координатах макета.** Экраны лежат внутри `#stage`,
то есть в той же системе координат 2597,4 × 1568,8, что и фреймы в Figma —
поэтому слои расставлены точными координатами оттуда, а не процентами.
Шапки на главном экране нет; на остальных экранах меню она осталась, как в макете.

Переходы: назад — по `data-back` на стрелке, вперёд — обработчиками кнопок.
Экран боя из потока экранов исключён: он показывается, когда все `.screen` скрыты
(`showScreen(null)`). Выйти в меню можно кнопкой «В МЕНЮ» на экране итога
или иконкой ⌂ в правом верхнем углу — она и кнопка лога ≡ видны только в бою
(класс `body.inmenu` снимает их, пока открыт любой экран).

**Чего в этих экранах НЕТ:**
- камеры и разбора QR (раздел 9, пункт 4). **Ручного ввода кода нет и не будет**:
  карта достаётся только сканированием физической карточки, иначе номера можно
  подобрать перебором. Когда камера появится — распознали QR, нашли ключ карты
  и вызвали `cardScanned(key)`, окно «Карта добавлена» уже готово;
- коллекции карт: «Карта добавлена» ничего не ограничивает, в редакторе по-прежнему
  доступны все 84 карты. Настоящая коллекция — отдельная задача;
- смены аватара: картинка одна, взята из старой игры.

**Как вернуть мультиплеер:** раскомментировать `<script src="net.js"></script>`
в конце `proto/index.html` и вернуть на экран `pvpScreen` кнопки создания и входа
по коду (разметка была в блоке `#pvpBox`, см. историю git).

---

## 14. Синхронный PvP на сервере (решённая архитектура)

Решение заказчика: **синхронный бой двух живых игроков, обсчёт боя и колоды — на сервере.**
Раздел описывает, как это устроено. Пока НЕ реализовано, это план.

Контекст площадки взят из ТЗ соседнего проекта «Оракул» (сервис в том же приложении):
WebView в МП «Клуб Друзей», МП передаёт данные пользователя при открытии, оплата только
через программу лояльности, состояние в продукте — на сервере, а не в localStorage.

**Вводные от заказчика:**
- ориентация экрана — как в прототипе (горизонтальная), портретной перерисовки не будет;
- **игра не шлёт уведомлений** — позвать игрока в бой нечем, оба должны быть в лобби;
- экономики сейчас нет;
- матчинг только обозначен, не продуман.

### 14.1 Лобби

Сервер держит присутствие: клиент шлёт «я здесь» раз в несколько секунд, молчание
дольше ~15 с — игрок пропадает из списка. В лобби показываются **все игроки, включая
себя** (своя строка помечена, без кнопки вызова). **Кодов комнат нет** — вызов по id
игрока из списка. Экраны «запрос на бой» и «ожидание второго игрока» из схемы (раздел 13)
нужны именно для этого.

Холодный старт остаётся риском: пока игроков мало, лобби будет пустым. Лечится пулом
или подбором — отложено вместе с матчингом.

### 14.2 Главный принцип

Клиент — **экран и джойстик**, а не движок. Он не считает бой: показывает присланный
сервером вид состояния и отправляет намерения:

```
{ seq: 12, t: "play", uid: 47 }   // сыграть карту из руки
{ seq: 13, t: "take", uid: 51 }   // взять новую карту над рукой
{ seq: 14, t: "end"  }            // конец хода
```

Всё остальное — следствия этого решения.

### 14.3 Защита от читеров

| Мера | Что закрывает |
|---|---|
| Сервер проверяет каждое действие: идёт ли бой, твой ли ход, не разыгрывается ли эффект, лежит ли `uid` в ТВОЕЙ руке, хватает ли энергии | Карты не из руки, игра без энергии и не в свой ход |
| **Туман войны:** про себя игрок получает всё, про соперника — только числа (HP, блок, сила, ловкость, энергия) и РАЗМЕРЫ стопок. Содержимое чужой руки и порядок любой колоды не уходят в браузер никогда | Подглядывание. Клиентский движок так не умеет в принципе — ему нужно всё состояние |
| Случайность только на сервере: тасовка, случайная карта для `copyHand`, позиция вставки `copyDeck`. Зерно пишется в запись матча, клиенту не отдаётся | Перекрутка тасовки до удобной |
| Числа карт берутся из серверной копии базы; `cards.js` на клиенте — только для отрисовки | Патч клиента «урон 6 → 60» |
| Возрастающий `seq` у каждого намерения, всё ≤ последнего применённого игнорируется | Двойная отправка и повтор пакета |
| Колода берётся из серверного хранилища, сервер сам проверяет 20 карт, ≤3 копий, наличие в коллекции | Нелегальные колоды и карты, которых нет |
| Подписанный id из WebView → сервер проверяет подпись → короткоживущий токен сессии | Подмена личности |
| Таймер хода на сервере, клиент его только рисует; истёк — сервер сам завершает ход | Затягивание и зависшие бои |
| Окно на переподключение, состояние матча живёт на сервере; несколько пропущенных ходов → техническое поражение | Выход при проигрыше; при этом честный обрыв не карается сразу |
| Ограничение частоты действий на соединение | Флуд и случайные циклы |

**Чего этим не закрыть** (честно, чтобы не было ложного ощущения безопасности):
- бот, играющий за человека — все его действия легальны, сервер их не отличит;
- договорные бои между двумя аккаунтами.

Обе проблемы становятся реальными только вместе с экономикой, а её пока нет.

### 14.4 Что это значит для кода

**Шаг 1 сделан: правила лежат в `proto/rules.js`** — без DOM, без глобального состояния.

Что даёт модуль серверу:
- `newState(deckA, deckB, fx)` — новый бой; `startTurn`, `endTurn`, `resolveCard` — ход;
- `canPlay(S, who, uid)` — **проверка нечестного клиента**: вернёт карту, только если
  идёт бой, ход именно этого игрока, карта лежит в ЕГО руке и энергии хватает;
- `spendCard`, `takePending` — применение проверенного действия;
- `viewFor(S, who)` — **туман войны**: про себя всё, про соперника только числа
  и размеры стопок, чужая рука и порядок колод наружу не уходят;
- `chooseCard` — скриптовый игрок, он же бот в PvE;
- источник случайности берётся из `fx.rnd`, поэтому на сервере он будет с зерном:
  бой воспроизводится по записи, а клиент не может перекрутить тасовку.

Интерфейс передаёт свой `fx` (лог, всплывающие числа, тряска, паузы), сервер —
`RULES.SILENT` и считает бой мгновенно и молча.

- `relay.js` из тупого релея становится сервером матчей;
- `proto/net.js` худеет: приём вида состояния и отправка намерений, расчёта не остаётся;
- приём с переворотом снимка («свои и чужие местами») переиспользуется — сервер так же
  готовит вид под каждого игрока;
- **бой с ботом остаётся на клиенте** как есть: там нечего защищать.

Транспорт: опрос раз в ~500 мс. Для пошаговой игры хватает и надёжнее в нестабильной сети
WebView. WebSocket даст мгновенность, но это отдельная сложность — начинаем с опроса.

### 14.5 Порядок работ

1. ~~Вынести правила из `index.html` в модуль без DOM~~ — **СДЕЛАНО**, `proto/rules.js`.
   Прототип работает как раньше, 71 проверка `proto-test.js` проходит.
2. ~~Сервер матчей: проверка действий, виды состояния, зерно, таймеры ходов~~ —
   **СДЕЛАНО**, `matches.js` + `/api/match/*` в `relay.js`, 55 проверок `match-test.js`.
3. ~~Клиент PvP: рисует присланный вид, шлёт намерения~~ — **СДЕЛАНО**, `proto/net.js`.
4. ~~Лобби с присутствием и вызовом по id~~ — **СДЕЛАНО**, `lobby.js` + `proto/net.js`.
5. ~~Идентификация из МП, колоды и коллекция на сервере~~ — **СДЕЛАНО**, `auth.js` + `store.js`.

### 14.6 Сервер матчей (`matches.js`) — сделано

Бой живёт здесь, и здесь ему верят. HTTP в файле нет — наружу его отдаёт `relay.js`.

| Запрос | Что делает |
|---|---|
| `POST /api/match/new` | создаёт бой; в ответ `matchId` и **два токена**, по одному стороне |
| `POST /api/match/state` | состояние глазами игрока: туман войны уже наложен |
| `POST /api/match/act` | намерение: `play` / `take` / `end` / `give` |

Что уже работает против нечестного клиента:

- **Сторона определяется по токену.** `playerId`, присланный клиентом, сам по себе
  не принимается: подделать его нельзя.
- **Каждое действие проверяется:** идёт ли бой, твой ли ход, не разыгрывается ли
  сейчас карта, лежит ли `uid` в ТВОЕЙ руке, хватает ли энергии. Не сошлось —
  отказ, состояние не меняется (проверено тестом: `rev` не растёт).
- **Номера действий** возрастают, повтор и устаревший пакет отбрасываются —
  двойным нажатием карту дважды не сыграть.
- **Колода проверяется при создании боя:** 20 карт, не больше 3 копий, все карты
  существуют. Кривая колода — бой не создаётся.
- **Зерно случайности** хранится в матче и наружу не отдаётся: по нему бой
  переигрывается при разборе, но клиент не может вычислить порядок колоды.
  Одно зерно даёт одинаковую раздачу — проверено тестом.
- **Таймер хода 90 секунд** идёт на сервере. Просрочен — сервер сам завершает ход
  и засчитывает пропуск; три пропуска подряд у одной стороны — техническое
  поражение. Это же закрывает уход из боя при проигрыше.
- Действия применяются строго по одному (очередь на матч), лог обрезается,
  все ходы пишутся в `m.moves` для разбора.

**Чего в шаге 2 намеренно нет** (это шаги 4 и 5):
- бой создаёт кто угодно — лобби ещё не сделано;
- колоды приходят от клиента, а не из серверного хранилища;
- личность игрока не подтверждена подписью из МП.

Тесты: `npm test` гоняет правила и сервер матчей, `npm run test:match` — только сервер.

### 14.7 Клиент PvP (`proto/net.js`) — сделано

Клиент **не считает бой совсем**. Делает две вещи: берёт у сервера вид состояния
и рисует его, отправляет намерения. Режим включается кнопкой на экране PvP.

- Вид приходит уже от лица игрока (`me` / `foe`, ход как `me`/`foe`), поэтому вся
  существующая отрисовка работает без правок — `toState()` только раскладывает вид
  в привычную форму `S.p[0]` / `S.p[1]`. Стопки соперника приходят числами
  и разворачиваются в пустые массивы нужной длины, потому что рисовалка берёт `.length`.
- Намерения уходят через `NET.send`: `play` / `take` / `end` / `give`. Ответа не ждём,
  следующий опрос принесёт новое состояние; локально рука запирается, чтобы
  не отправить действие дважды.
- Опрос раз в 500 мс. Пока карту тянут пальцем, состояние не подменяется, иначе жест рвётся.
- Таймер хода показывается в шапке вместо подписи «прототип дуэли».
- Карта, сыгранная соперником, показывается в центре: сервер отдаёт `lastPlay`
  с растущим номером, клиент по нему понимает, что карта новая.
- Выход из боя (⌂, «В МЕНЮ», «ЕЩЁ РАЗ») отправляет `give` — иначе соперник ждал бы
  до истечения таймеров.
- Победы и поражения PvP пишутся в профиль здесь же, один раз за бой.

Игрок опознаётся случайным номером из `localStorage` (`atlanteans_player`),
имя берётся из профиля. До интеграции с «Клубом Друзей» этого хватает (шаг 5).

### 14.8 Лобби (`lobby.js`) — сделано

Уведомлений у игры нет, поэтому позвать человека можно только пока он смотрит
в лобби. Отсюда всё устройство.

| Запрос | Что делает |
|---|---|
| `POST /api/lobby` | «я здесь» + весь снимок: список, вызовы, пара, готовый бой |
| `POST /api/invite` | вызвать игрока по id |
| `POST /api/answer` | ответить на вызов: да или отказ |
| `POST /api/ready` | я выбрал колоду; когда готовы оба — рождается бой |
| `POST /api/leave` | выйти из пары или из боя обратно в лобби |

- **Присутствие** держится опросом раз в секунду; молчание дольше 15 с — игрок
  пропадает из списка. Вызов живёт 30 с, брошенная пара — 5 минут.
- **В списке все, включая себя:** своя строка помечена «ЭТО ВЫ» и без кнопки
  вызова (решение заказчика). Занятые помечены и вызову не подлежат.
- **Кодов комнат нет** — вызывают по id из списка.
- Путь: лобби → вызов → согласие → выбор колоды → ожидание второго → бой.
  Ровно эти экраны нарисованы в схеме, включая окна «запрос» и «ожидание».
- Отказ, уход соперника и пропажа из сети разводят обоих обратно в лобби
  с понятным сообщением.

**Как проверить вдвоём:** `node relay.js`, открыть `http://localhost:3000/proto/`
в двух окнах (второе — приватное, иначе номер игрока совпадёт), в обоих PvP.
Друг у друга в списке — жать «ВЫЗВАТЬ».

---

### 14.9 Личность и хранилище (`auth.js`, `store.js`) — сделано

**Личность.** В продукте id приходит из «Клуба Друзей»: приложение открывает
WebView и передаёт ПОДПИСАННЫЙ токен. Сервер проверяет подпись и выдаёт токен
сессии; дальше каждый запрос идёт с ним. **`playerId` от клиента не принимается
нигде** — иначе любой сыграл бы за другого.

**ТЗ для команды МП** (это и есть то, что просили в разделе 9, пункт 3):

- игра открывается по адресу `<сервер>/proto/?u=<токен>`;
- токен: `base64url(JSON)` + `"."` + `base64url(HMAC-SHA256(base64url(JSON), секрет))`;
- внутри JSON: `{ "id": "<id в Клубе Друзей>", "name": "<имя>", "exp": <мс epoch> }`;
- `exp` — срок годности, рекомендуем 5 минут: токен нужен один раз при открытии;
- секрет общий, у нас лежит в переменной окружения `ATL_SECRET`;
- подпись сверяется сравнением постоянного времени, просроченный токен отвергается.

**Режим разработки:** если `ATL_SECRET` не задан, сервер принимает id без подписи —
так гоняется прототип локально. **На боевом стенде секрет задать обязательно**,
иначе личность не проверяется вовсе.

**Хранилище.** У игрока на сервере лежат коллекция карт и колоды.

- Колода в бой берётся **только из серверного хранилища** по номеру; клиент
  называет номер, а не состав. Прислать колоду из карт, которых у него нет,
  невозможно — это последняя дыра, которую закрывал шаг 5.
- Колода проверяется строже, чем в `matches.js`: не только 20 карт и лимит
  3 копии, но и наличие каждой карты в коллекции.
- **Коллекция пока выдаётся полная** (все 84 карты): сканирования QR ещё нет,
  ограничивать нечем. Переключатель `START_COLLECTION` в `store.js` — когда
  заработает камера, ставим `'starter'`, и коллекция начнёт наполняться картами
  из QR через `store.addCard()`.
- Хранится в `data/players.json` (в git не попадает). Для прототипа хватает;
  на боевом стенде это место заменит база — все обращения собраны в `store.js`.
  **Важно:** на Railway диск не переживает передеплой, там файл — временное решение.

Клиент: колоды с сервера, редактор сохраняет туда же, имя из профиля видно
соперникам в лобби. Колоды, собранные до появления сессии, один раз переносятся
на сервер. Без сервера (например, с githack) игра работает как раньше: колоды
в localStorage, бой с ботом.

---

_Последнее обновление: см. историю git. Актуальное состояние — всегда в коде, этот файл —
карта, а не замена чтению `proto/index.html` и `proto/cards.js`._
